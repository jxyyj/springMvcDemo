<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yyj.springmvc.mapper.StudentMapper">
    <resultMap id="BaseResultMap" type="com.yyj.springmvc.entity.Student">
        <id column="sid" property="sid" jdbcType="VARCHAR"/>
        <result column="sname" property="sname" jdbcType="VARCHAR"/>
        <result column="sage" property="sage" jdbcType="INTEGER"/>
        <result column="ssex" property="ssex" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        sid, sname, sage, ssex
    </sql>

    <select id="selectAll" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from student
    </select>

    <select id="selectById" parameterType="java.lang.String" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from student
        where sid = #{sid,jdbcType=VARCHAR}
    </select>

    <insert id="insert" parameterType="com.yyj.springmvc.entity.Student">
        insert into student (sid, sname, sage, ssex)
        values (#{sid,jdbcType=VARCHAR}, #{sname,jdbcType=VARCHAR}, #{sage,jdbcType=INTEGER}, #{ssex,jdbcType=VARCHAR})
    </insert>

    <update id="updateById" parameterType="com.yyj.springmvc.entity.Student">
        update student
        set sname = #{sname,jdbcType=VARCHAR},
            sage = #{sage,jdbcType=INTEGER},
            ssex = #{ssex,jdbcType=VARCHAR}
        where sid = #{sid,jdbcType=VARCHAR}
    </update>

    <delete id="deleteById" parameterType="java.lang.String">
        delete from student
        where sid = #{sid,jdbcType=VARCHAR}
    </delete>
</mapper>
